# ╔══════════════════════════════════════════════════════════╗
# ║         VegaMCP v3.2 — Environment Configuration        ║
# ╚══════════════════════════════════════════════════════════╝
#
# Copy this file to .env and fill in your API keys.
# Only the keys you need are required — everything else is optional.

# ─── Core Reasoning (at least one required for reasoning tools) ───
OPENROUTER_API_KEY=                         # Best: supports ALL models via one key
DEEPSEEK_API_KEY=                           # Direct DeepSeek API (R1 + Chat)

# ─── Kimi / Moonshot AI (K2.5, 262K context) ───
KIMI_API_KEY=                               # api.kimi.com (Kimi Code)

# ─── Google Gemini ───
GEMINI_API_KEY=                             # ai.google.dev (Gemini 2.0/2.5)

# ─── Groq (ultra-fast inference) ───
GROQ_API_KEY=                               # console.groq.com

# ─── Mistral AI ───
MISTRAL_API_KEY=                            # console.mistral.ai

# ─── Together AI ───
TOGETHER_API_KEY=                           # api.together.xyz

# ─── xAI (Grok) ───
XAI_API_KEY=                                # console.x.ai

# ─── Local Ollama (free inference, zero latency) ───
OLLAMA_URL=http://localhost:11434           # Default Ollama URL

# ─── Sentry Error Tracking (optional) ───
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# ─── GitHub Scraper (optional, increases rate limit 60→5000/hr) ───
GITHUB_TOKEN=

# ─── Web Search (at least one for web_search tool) ───
TAVILY_API_KEY=                             # Primary: AI-optimized search
SEARXNG_URL=http://localhost:8080           # Fallback: self-hosted search

# ─── Token Budget (optional, defaults shown) ───
TOKEN_DAILY_BUDGET_USD=5.00                 # Max daily spending
TOKEN_HOURLY_BUDGET_USD=1.00                # Max hourly spending

# ─── Tool Profile (controls which tools are exposed) ───
# Options: full | minimal | research | coding | ops
# full     = ALL tools (63 tools) — default
# minimal  = Memory + Reasoning + Budget + Knowledge + Prompts (10 tools)
# research = minimal + Browser + Web Search + GitHub + Agent tools (28 tools)
# coding   = minimal + GitHub + Sandbox + Code Analysis + Agent tools (25 tools)
# ops      = minimal + Swarm + Watchers + Webhooks + Scheduling + Agent tools (32 tools)
#
#                 Skills Engine (vegamcp_skills), Seed Data — included in all profiles
VEGAMCP_TOOL_PROFILE=full

# ─── General ───
WORKSPACE_ROOT=./                           # Your workspace root
DATA_DIR=./data                             # SQLite + vector store location
