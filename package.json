{
  "name": "vegamcp",
  "version": "6.0.0",
  "description": "VegaMCP v6.0 — Protocol Supremacy Edition. 60+ tools, 10-agent swarm, A2A protocol, zero-trust identity, MCP gateway, async tasks (SEP-1686), OAuth 2.1, agent graphs, agentic sampling v2, multimodal embeddings, dynamic indexing, MCP Apps (UI), tool search, structured output, AI elicitation, session manager.",
  "main": "build/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "tsx watch src/index.ts",
    "clean": "rmdir /s /q build 2>nul & echo cleaned",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^16.6.1",
    "playwright": "^1.50.0",
    "sql.js": "^1.11.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
